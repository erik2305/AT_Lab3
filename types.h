#ifndef TYPES_H
#define TYPES_H

// Define VarType here
enum class VarType {
    UINT,
    BOOLEAN,
    // Add array types if necessary
};

// Forward declarations or other shared types can be added here

#endif // TYPES_H
